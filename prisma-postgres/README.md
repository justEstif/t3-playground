# Prisma

Node API + PostgreSQL

- jokes table
- tags table
- user (joker)

- a user has jokes (one to many)
- a single tags has multiple jokes
- a single jokes has multiple jokes
- multiple tags have multiple jokes
  - pivot table

- what is prisma/client?
  - type safe
- where is prisma best used?
  - node API
  - serverless API (next.js api routes)



DATABASE_URL="postgresql://estif:121234@localhost:5432/myDatabaseName"
